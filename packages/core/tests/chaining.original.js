let observable$

observable$.subscribe()

observable$.subscribe().unsubscribe()

observable$.subscribe(() => ({
	complete: Function.prototype,
	error: Function.prototype,
	next: (t) => {
		Function.prototype(t)
	},
}))

observable$.subscribe()
.unsubscribe()

observable$.subscribe()

.unsubscribe()

observable$

.subscribe()

.unsubscribe()

observable$.subscribe()[0]['unsubscribe']()['length']['ball']()

observable$.subscribe()[0].unsubscribe()

Object.keys(observable$)

let d3

d3.select("body").selectAll("p").data([4, 8, 15, 16, 23, 42 ])

d3.select("body")
.selectAll("p").data([4, 8, 15, 16, 23, 42 ])

d3.select("body")
.selectAll("p")
.data([4, 8, 15, 16, 23, 42 ])

d3
.select("body")
.selectAll("p")
.data([4, 8, 15, 16, 23, 42 ])

let array = []

array[0]

array[0][1]

array[0][1].length

array.map(({
	foo,
	bar,
}) => ({
	foo,
	bar,
})).filter(Boolean)

array.map(({
	foo,
	bar,
}) => ({
	foo,
	bar,
}))
.filter(Boolean)

array.map(({
	foo,
	bar,
}) => ({
	foo,
	bar,
})).filter(Boolean).length

array.map(({
	foo,
	bar,
}) => ({
	foo,
	bar,
})).filter(Boolean)
.length

array.forEach(() => ({
	value: 'value',
}))

void [1,2,3].forEach(Function.prototype)

void [1,2,3].forEach(Function.prototype).length
